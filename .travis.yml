language: java
jdk:
  - oraclejdk9
addons:
  sonarcloud:
    organization: "refactoring-bot" # the key of the org you chose at step #3
cache:
  directories:
  - "$HOME/.m2/repository"
  - "$HOME/.sonar/cache"
before_install:
  - chmod +x mvnw
script:
  # other script steps might be done before running the actual SonarQube analysis
  - ./mvnw test -B
  - sonar-scanner
