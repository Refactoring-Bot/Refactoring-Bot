spring:
  application:
    name: alternativerefactoringbot
  datasource:
    url: jdbc:mysql://localhost:3306/gitconfig_db?verifyServerCertificate=false&useSSL=true
    username: root
    password: root
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
  profiles:
    active: "dev"
server:
  port: 8808
---
spring:
  profiles: dev
bot:
  botRefactoringDirectory: C:/Users/stefa/Bachelorarbeit-Repos/BotForks/